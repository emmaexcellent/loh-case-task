import { View, Text } from 'react-native'
import React from 'react'
import CameraViewScreen from '@/components/ui/Camera'

const CameraScreen = () => {
  return (
    <CameraViewScreen/>
  )
}

export default CameraScreen